import section from '../base-section';
import styled from 'styled-components';

export default styled(section)`
    .container {
        display: flex;
    }

    .right {
        margin-left: 14rem;
        padding-right: 6rem;
    }
`;
