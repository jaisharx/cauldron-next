import section from '../base-section';
import styled from 'styled-components';

export default styled(section)`
    .container {
        display: flex;
    }

    .left {
        padding-right: 14rem;
    }
`;
